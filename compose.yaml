services:
  shubert:
    build:
      context: .
      dockerfile: Dockerfile
    image: dobrikov91/shubert:latest
    restart: unless-stopped
    privileged: true
    ports:
      - "8088:8088"
    volumes:
      - /dev:/dev
      - ./data:/app/data
    devices:
      - /dev/snd:/dev/snd
